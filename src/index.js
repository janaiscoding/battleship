import { createBoard, placeShip } from "./factories/gameboard";
//board, length, vert coord, hor coord, direct
let testBoard = createBoard(10);
// placeShip(testBoard, 3, 5, 0, "H");
console.log(testBoard);
// placeShip(testBoard, 5, 3, 0, "V");
